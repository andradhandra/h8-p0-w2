Tugas Pseudocode Ridza 

1. Second Law of Newton Algorithm Calculation

STORE 'mass' = 600
STORE 'cceleration' = 2

CALCULATE 'mass' * 'acceleration' 
SET 'force' value from CALCULATE

DISPLAY 'force'


2. Leap Year Algorithm

STORE 'year' with any value

IF 'year' mod 4 == 0 AND 'year' mod 100 != 0 
    IF 'year' mod 4 == 0 AND 'year' mod 100 == 0 AND 'year' mod 400 == 0
        DISPLAY "Tahun 'year' adalah tahun kabisat"
    ENDIF
ELSE 
    DISPLAY "Tahun 'year' BUKAN tahun kabisat"
ENDIF  


3. Foxie Laundry Day Algorithm

STORE 'laundry' = 0

WHILE 'laundry' < 20
    SET 'laundry' with 'laundry' + 1
ENDWHILE

DISPLAY "Nyalakan mesin cuci"


4. Periksa Kuku Algorithm

STORE 'siswa' = 0
STORE 'kukuPanjang' as boolean

WHILE 'siswa' < 40
    IF 'kukuPanjang' = "true"
        DISPLAY "hukum"
    ELSE DISPLAY "pujian"
    ENDIF
   
    SET 'siswa' with 'siswa' + 1
ENDWHILE